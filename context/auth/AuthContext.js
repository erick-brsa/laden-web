
import { createContext } from 'react';
// import { IUser } from '../../interfaces';

// interface ContextProps {
    isLoggedIn: boolean;
    // user?: IUser;

//     loginUser: (email, password) => Promise<boolean>;
//     registerUser: (name, email, password) => Promise<{ hasError; message?; }>;
//     logout: () => void;
// }


export const AuthContext = createContext({});